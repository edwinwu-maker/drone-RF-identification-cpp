# LLM Wiki

一种使用 LLM 构建个人知识库的模式。

这是一个想法文件，设计用途是复制粘贴给你自己的 LLM Agent，例如 OpenAI Codex、Claude Code、OpenCode / Pi 等。它的目标是传达高层思路，具体细节则由你的 agent 与你协作构建。

## 核心思路

大多数人使用 LLM 和文档的体验类似 RAG：你上传一组文件，LLM 在查询时检索相关片段，然后生成答案。这种方式有效，但 LLM 每次回答问题时都在从零重新发现知识。知识没有积累。问一个需要综合五份文档的细微问题时，LLM 每次都必须找到并拼接相关片段。没有任何东西被沉淀下来。NotebookLM、ChatGPT 文件上传功能以及大多数 RAG 系统都是这样工作的。

这里的想法不同。不要只在查询时从原始文档中检索，而是让 LLM **incrementally builds and maintains a persistent wiki**：一组结构化、相互链接的 Markdown 文件，位于你和原始资料之间。当你添加一个新来源时，LLM 不只是为以后检索而索引它。它会阅读它，提取关键信息，并把它整合进现有 wiki：更新实体页面，修订主题摘要，标注新数据与旧说法相矛盾的地方，加强或挑战正在演化的综合结论。知识被编译一次，然后被*持续保持最新*，而不是在每次查询时重新推导。

关键差异在于：**the wiki is a persistent, compounding artifact.** 交叉引用已经存在。矛盾已经被标出。综合结论已经反映了你读过的一切。你每添加一个来源、每提出一个问题，wiki 都会变得更丰富。

你不会，或者很少，亲自编写 wiki。LLM 会编写并维护全部内容。你负责来源、探索和提出正确的问题。LLM 负责所有繁琐工作：总结、交叉引用、归档和记账，这些工作让知识库随着时间推移真正有用。实践中，我会一边打开 LLM agent，一边打开 Obsidian。LLM 根据我们的对话进行编辑，我则实时浏览结果：跟随链接、检查图谱视图、阅读更新后的页面。Obsidian 是 IDE；LLM 是程序员；wiki 是代码库。

这种模式可以应用到许多不同场景。几个例子：

- **Personal**：跟踪自己的目标、健康、心理、自我提升；归档日记、文章、播客笔记，并随着时间积累出一幅结构化的自我画像。
- **Research**：用几周或几个月深入研究一个主题；阅读论文、文章、报告，并增量构建一个全面的 wiki，其中包含不断演化的论点。
- **Reading a book**：边读边归档每一章，为角色、主题、情节线以及它们之间的联系建立页面。读完时，你会拥有一个丰富的伴读 wiki。可以想想 [Tolkien Gateway](https://tolkiengateway.net/wiki/Main_Page) 这样的粉丝 wiki：成千上万个相互链接的页面，涵盖角色、地点、事件、语言，由志愿者社区多年共建。你可以在阅读时为自己构建类似的东西，由 LLM 负责所有交叉引用和维护。
- **Business/team**：由 LLM 维护的内部 wiki，输入来自 Slack 线程、会议转录、项目文档、客户通话。也可以让人参与审阅更新。wiki 能保持最新，因为 LLM 会做团队里没人想做的维护工作。
- **Competitive analysis, due diligence, trip planning, course notes, hobby deep-dives**：任何你会长期积累知识，并希望它被组织起来而不是散落各处的场景。

## 架构

有三层：

**Raw sources**：你精心整理的一组源文档。文章、论文、图片、数据文件。这些内容不可变。LLM 从中读取，但绝不修改它们。这是你的事实来源。

**wiki**：一个由 LLM 生成的 Markdown 文件目录。包括摘要、实体页面、概念页面、对比、概览、综合分析。LLM 完全拥有这一层。它创建页面，在新来源到来时更新页面，维护交叉引用，并保持所有内容一致。你阅读它；LLM 编写它。

**schema**：一份文档，例如 Claude Code 使用的 CLAUDE.md 或 Codex 使用的 AGENTS.md，用来告诉 LLM 这个 wiki 如何组织、有哪些约定，以及在摄取来源、回答问题或维护 wiki 时应遵循什么工作流。这是关键配置文件：它让 LLM 成为一个有纪律的 wiki 维护者，而不是一个通用聊天机器人。随着你们逐渐弄清楚什么适合自己的领域，你和 LLM 会共同演化这份文档。

## 操作

**Ingest.** 你把一个新来源放入原始资料集合，然后告诉 LLM 处理它。一个示例流程是：LLM 阅读来源，与你讨论关键收获，在 wiki 中写一页摘要，更新索引，更新整个 wiki 中相关的实体和概念页面，并在日志中追加一条记录。单个来源可能触及 10 到 15 个 wiki 页面。就我个人而言，我更喜欢一次摄取一个来源并保持参与：我阅读摘要，检查更新，并指导 LLM 强调什么。但你也可以用较少监督一次批量摄取多个来源。你需要发展出适合自己风格的工作流，并把它记录到 schema 中，供未来会话使用。

**Query.** 你针对 wiki 提问。LLM 搜索相关页面，读取它们，并用引用综合出答案。答案可以根据问题采用不同形式：Markdown 页面、对比表、幻灯片（Marp）、图表（matplotlib）、画布。重要洞见是：**good answers can be filed back into the wiki as new pages.** 你要求的一个对比、一段分析、你发现的一个联系，都是有价值的，不应该消失在聊天记录里。这样，你的探索会像摄取的来源一样在知识库中复利增长。

**Lint.** 定期让 LLM 对 wiki 做健康检查。检查内容包括：页面之间的矛盾、被较新来源取代的过时说法、没有入链的孤立页面、被提到但缺少独立页面的重要概念、缺失的交叉引用、可以通过网页搜索填补的数据空白。LLM 很擅长提出值得调查的新问题和需要寻找的新来源。这能让 wiki 在增长过程中保持健康。

## 索引与日志

两个特殊文件可以帮助 LLM，也帮助你，在 wiki 增长后进行导航。它们承担不同目的：

**index.md** 面向内容。它是 wiki 中所有内容的目录：每个页面列出链接、一句话摘要，以及可选的元数据，例如日期或来源数量。它按类别组织，例如实体、概念、来源等。LLM 在每次摄取时更新它。回答查询时，LLM 首先读取索引以找到相关页面，然后深入这些页面。在中等规模下，这种方式出奇地好用，例如约 100 个来源、数百个页面，并且避免了对基于 embedding 的 RAG 基础设施的需求。

**log.md** 面向时间。它是一个只追加的记录，说明发生了什么以及何时发生：摄取、查询、检查。一个有用的小技巧是：如果每条记录都以一致前缀开头，例如 `## [2026-04-02] ingest | Article Title`，日志就可以用简单的 Unix 工具解析。`grep "^## \[" log.md | tail -5` 会给出最近 5 条记录。日志提供 wiki 演化的时间线，也帮助 LLM 理解最近完成了什么。

## 可选：CLI 工具

到某个阶段，你可能会想构建一些小工具，帮助 LLM 更高效地操作 wiki。最明显的是一个面向 wiki 页面搜索的搜索引擎。在小规模时索引文件足够，但随着 wiki 增长，你会想要真正的搜索。[qmd](https://github.com/tobi/qmd) 是一个不错的选择：它是面向 Markdown 文件的本地搜索引擎，具备混合 BM25/向量搜索和 LLM 重排序，全部在本机运行。它既有 CLI，因此 LLM 可以 shell 调用它，也有 MCP server，因此 LLM 可以把它作为原生工具使用。你也可以自己构建更简单的东西：当需求出现时，LLM 可以帮助你 vibe-code 一个朴素的搜索脚本。

## 提示与技巧

- **Obsidian Web Clipper** 是一个浏览器扩展，可以把网页文章转换成 Markdown。它非常适合快速把来源放进你的原始资料集合。
- **Download images locally.** 在 Obsidian 设置 -> 文件与链接中，把“附件文件夹路径”设置为一个固定目录，例如 `raw/assets/`。然后在设置 -> 快捷键中，搜索“Download”，找到“Download attachments for current file”，并绑定到一个快捷键，例如 Ctrl+Shift+D。剪藏一篇文章后，按下快捷键，所有图片都会被下载到本地磁盘。这是可选的，但很有用：它让 LLM 可以直接查看和引用图片，而不是依赖可能失效的 URL。注意，LLM 不能原生地一次性阅读带有内联图片的 Markdown。解决办法是让 LLM 先读文本，然后再单独查看部分或全部被引用图片，以获得额外上下文。这有点笨拙，但效果足够好。
- **Obsidian's graph view** 是观察 wiki 形状的最佳方式：什么和什么相连，哪些页面是枢纽，哪些页面是孤立的。
- **Marp** 是一种基于 Markdown 的幻灯片格式。Obsidian 有对应插件。它适合直接从 wiki 内容生成演示文稿。
- **Dataview** 是一个 Obsidian 插件，可以对页面 frontmatter 运行查询。如果你的 LLM 给 wiki 页面添加 YAML frontmatter，例如标签、日期、来源数量，Dataview 就可以生成动态表格和列表。
- wiki 本质上只是一个由 Markdown 文件组成的 git 仓库。你自然获得版本历史、分支和协作能力。

## 为什么这有效

维护知识库中繁琐的部分不是阅读或思考，而是记账。更新交叉引用，保持摘要最新，记录新数据何时与旧说法矛盾，在几十个页面之间维持一致性。人类会放弃 wiki，是因为维护负担比价值增长得更快。LLM 不会厌烦，不会忘记更新交叉引用，并且可以一次触及 15 个文件。wiki 能保持维护，是因为维护成本接近于零。

人的工作是筛选来源、指导分析、提出好问题，并思考这一切意味着什么。LLM 的工作是除此之外的一切。

这个想法在精神上与 Vannevar Bush 的 Memex（1945）有关：一个个人的、精心策划的知识存储，在文档之间有联想路径。Bush 的愿景比后来的 Web 更接近这里描述的模式：私密、主动策划，文档之间的连接与文档本身同样有价值。他当时无法解决的问题是由谁来维护。LLM 解决了这一点。

## 说明

本文档有意保持抽象。它描述的是想法，而不是某个具体实现。确切的目录结构、schema 约定、页面格式、工具都会取决于你的领域、你的偏好以及你选择的 LLM。上面提到的一切都是可选且模块化的：选择有用的，忽略没用的。例如：你的来源可能全是文本，因此完全不需要图片处理。你的 wiki 可能足够小，索引文件就够了，不需要搜索引擎。你可能不关心幻灯片，只想要 Markdown 页面。你也可能想要一套完全不同的输出格式。正确的使用方式是把它分享给你的 LLM agent，并一起实例化一个适合你需求的版本。本文档唯一的任务是传达这种模式。剩下的，你的 LLM 可以自己弄清楚。
